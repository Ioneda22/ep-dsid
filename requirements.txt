# PeerSpot — dependências 
# Tudo o mais usado é stdlib: socket, threading, sqlite3, hashlib, json, logging, uuid.

# API peer <-> tracker (REST sobre HTTP, async nativo)
fastapi>=0.115
uvicorn[standard]>=0.32

# Mensagens do protocolo (schema + validação em src/common/messages.py)
pydantic>=2.0

# Configuração (config/*.yaml)
pyyaml>=6.0

# Testes
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27          # cliente HTTP nos testes de integração do tracker
